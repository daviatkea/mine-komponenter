---
const { as, level } = Astro.props;

const Tag = as;

const options = {
  "1": "text-5xl font-bold",
  "2": "text-4xl",
  "3": "text-3xl",
  "4": "text-2xl",
  "5": "text-xl",
};

const classes = options[level];
---

<Tag class={classes}>
  <slot />
</Tag>
